import React from 'react';

const ErrorMsg = () => (
    <div>
        <p 
            id="errorMsg" 
            className="errorMsg" 
            name="errorMsg" 
        >The username and password fields cannot be empty</p>
    </div>
);

export default ErrorMsg;